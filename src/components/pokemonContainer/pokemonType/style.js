import styled from 'styled-components';

export const TypeLabel = styled.Text`
  background-color: ${p => p.color};
  border-radius: 5px;
  padding: 2px;
`;

export const AlignBaseline = styled.View`
  align-items: baseline;
`;
